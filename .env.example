# Brave Search API key — https://api.search.brave.com/
BRAVE_API_KEY=your-key-here

# Google Sheets (gog CLI uses the account configured at setup)
# Sheet ID is created on first run and printed to stdout
SHEET_ID=

# Optional: override search settings
MAX_RESULTS_PER_QUERY=10
FRESHNESS=pw          # pd=past day, pw=past week, pm=past month
